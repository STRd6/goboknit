- game = this

.game
  .stats
    .money
      .title
        Money
      ¢
      = @money
  .fibers
    .title Fibers
    - each @purchasableFibers, (fiber) ->
      .fiber
        .price
          ¢
          = fiber.price
        .type= fiber.name
        .weight= "#{fiber.weight} kg"
        %button.purchase Purcase!
          - on "click", ->
            - game.purchase(fiber)

  .inventory
    - each @inventory, (item) ->
      .item
        .type= item.name
        .weight= item.weight

  .cyborgs Cyborgs
